import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PackagesubscribedPage } from './packagesubscribed';

@NgModule({
  declarations: [
    PackagesubscribedPage,
  ],
  imports: [
    IonicPageModule.forChild(PackagesubscribedPage),
  ],
})
export class PackagesubscribedPageModule {}
